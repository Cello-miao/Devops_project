### Copy this file to `.env.prod` and fill values before running docker-compose -f docker-compose.prod.yml up

# Postgres
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgrespassword
POSTGRES_DB=signin_project_prod

# Phoenix
SECRET_KEY_BASE=please_generate_a_secret_key_base
PORT=4000
HOST=example.com

# Frontend (optional) — if your frontend needs an env var for API base
VITE_API_BASE_URL=https://example.com/api

# Docker registry (used by CI)
DOCKER_REGISTRY=registry.example.com
DOCKER_IMAGE_PREFIX=yourproject
